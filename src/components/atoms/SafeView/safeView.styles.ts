import { LightAppColors } from '@/theme/variables'
import { StyleSheet } from 'react-native'

const safeViewStyles = StyleSheet.create({
	container: {
		backgroundColor: LightAppColors.cosmicLatte,
	},
})

export default safeViewStyles
