import { LightAppColors } from '@/theme/variables'
import { StyleSheet } from 'react-native'

const formTextInputStyles = StyleSheet.create({
	textInput: {
		padding: 10,
		borderWidth: 1,
		borderColor: LightAppColors.lowEmphasis,
		fontSize: 16,
	},
})

export default formTextInputStyles
